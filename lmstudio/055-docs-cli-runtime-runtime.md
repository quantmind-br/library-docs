---
title: '`lms runtime`'
url: https://lmstudio.ai/docs/cli/runtime/runtime
source: sitemap
fetched_at: 2026-04-07T21:28:01.759566009-03:00
rendered_js: false
word_count: 73
summary: This document provides a set of commands for using the `lms runtime` tool, detailing how to list, download, select, remove, and update inference runtimes.
tags:
    - runtime-management
    - command-line
    - inference-runtimes
    - lms-tool
category: reference
---

Use `lms runtime` to list, download, switch, or remove inference runtimes without opening the app.

### Commands[](#commands)

- `lms runtime ls` — list installed runtimes.
- `lms runtime get` — download a runtime.
- `lms runtime select` — set the active runtime.
- `lms runtime remove` — uninstall a runtime.
- `lms runtime update` — update an installed runtime.

### List installed runtimes[](#list-installed-runtimes)

```

lms runtime ls
```

### Download a runtime[](#download-a-runtime)

```

lms runtime get
```

### Switch to a runtime[](#switch-to-a-runtime)

```

lms runtime select
```

Follow the interactive prompts to choose the version you want.