---
title: Incorrect bounce angle against wall
url: https://ampcode.com/threads/T-019bbd1e-33ea-705d-b03b-5084ca3532ce
source: crawler
fetched_at: 2026-02-06T02:09:46.474002873-03:00
rendered_js: false
word_count: 3044
summary: This document describes the iterative development and debugging process for a 2D physics engine, focusing on collision detection logic for circular walls and gap edges.
tags:
    - physics-simulation
    - collision-detection
    - javascript-development
    - debugging-tools
    - swept-collision
    - 2d-physics
category: guide
---

[](#message-0-block-0 "Link to this block")

[![Lewis](https://workoscdn.com/images/v1/Yn1Ap8iIzuIc-drIAty3iJN55VNjtzxV1C1IHq2R518)](https://ampcode.com/@lewis-metcalf)

following: [019bbce6](https://ampcode.com/threads/T-019bbce6-9ccd-7538-896b-9909bc68175f) http://127.0.0.1:3000/playground.html?scenario=1&vx=6.00&vy=1.47 this angle looks incorrect - it should be bouncing against the wall here and so should have a faily straight bounce angle

[](#message-1-block-0 "Link to this block")

The user is referencing a thread and saying that the bounce angle looks incorrect when a ball bounces against a wall. Let me first read the referenced thread to understand the context, then look at the physics code to understand the issue.

[](#message-1-block-1 "Link to this block")

The user requested to build a static experimentation/playground environment for debugging gap collision logic. This playground should be in a separate file/endpoint, not animate, display trajectory via shadow balls, and allow setting up specific test cases.

### Initial Build Steps (Playground Setup)

**Files Created:**

- `/Users/lewis/git/demo/physics.js`: Shared physics constants and functions.
- `/Users/lewis/git/demo/playground.js`: Static experimentation environment.
- `/Users/lewis/git/demo/playground.html`: Entry point for the playground.

**`physics.js` Content Highlights (Initial):**

- Contains `isInGap` and `step` functions.
- The initial `step` function handles collision against the circular wall, but if `inGap` is true, it sets `newBall.passedThroughGap = true` instead of bouncing.

### Physics Refinement: Introducing Gravity and Trajectory Simulation

Gravity was added to the simulation, and the simulation function was updated to accept a `steps` count.

**`physics.js` Update (Step function):**

- Added `gravity` parameter to `step`.
- Applied gravity to `vy`: `vy: ball.vy + gravity`.

**`playground.js` Update (Scenarios & Simulation Call):**

- Scenarios were updated to include a `gravity` setting per test case.
- The `draw()` function was updated to pass the scenario's gravity value to `Physics.simulate`.
- Trajectory balls are colored based on state: Red=normal, Yellow=wall collision, Orange=edge collision, Green=passed through gap.

### Physics Refinement: Edge Collision Logic (Corner Bounce)

The user reported the ball was not bouncing correctly off the gap edges. The physics was updated to treat gap edges as points and resolve collisions there before checking the main wall collision.

**`physics.js` Update (Collision Resolution in `step`):**

- **New Logic:** Checks if the ball angle is near a gap edge (`edgeThreshold = 0.26` radians).
- **Edge Collision Response:**
  
  - If near an edge, the normal is calculated from the edge point to the ball center (`nx = edgeDx / edgeDist`).
  - Velocity is reflected using the standard elastic bounce formula (`v' = v - 2(v·n)n`).
  - Repositioning is done relative to the edge point: `newBall.x = edgeX + (newBall.radius + 1) * nx;`.
  - A new property `newBall.hitEdge` is set.
- **Wall Collision Response (Else block):** Repositioning was corrected to use the wall normal (`-nx`) instead of the center-to-ball vector (`-dx/distFromCenter`).

**`playground.js` Update (UI & Legend):**

- Legend updated to show three states: Wall collision (Yellow), Edge collision (Orange), Passed through gap (Green).
- Added debug info to show the calculated closest distance to edge points.

### Physics Refinement: Swept Collision Detection

After initial testing, the ball was tunneling through the edge points at high speed. The collision detection was updated to use swept collision against the edge points.

**`physics.js` Update (New `closestPointOnSegment` function added):**

- Implements logic to find the closest point on the line segment defined by the ball's previous position (`ball.x`, `ball.y`) and current position (`newBall.x`, `newBall.y`) to the edge point.

**`physics.js` Update (Collision Detection in `step`):**

- Edge collision check now uses the closest point on the *movement segment* to the edge point, checking if `dist <= newBall.radius`.
- Added an `approachDot` check to ensure the ball is moving *toward* the edge before reflecting.
- Repositioning for edge collision is now based on the collision point on the path, not just the edge point.

### Playground Update: Scenario Adjustment

Scenario 4 was adjusted to aim the ball directly at the bottom edge point to ensure the new swept collision logic is tested.

### Oracle/Librarian Consultation

Consultation confirmed the correct approach for circle-point collision: treat the endpoint as a zero-radius circle, calculate the normal from the point to the ball center, reflect velocity, and reposition the ball along that normal by its radius.

* * *

**Fixes applied in subsequent steps:**

1. **Fixing Gap Passage Collision (Teleportation Bug 1):** Added a check to skip edge collision processing if the ball started the frame outside the circle (`wasInside` check in edge collision logic).
2. **Fixing Wall Collision Outside Circle (Teleportation Bug 2):** Added a check to skip wall collision processing if the ball started the frame outside the circle (`wasInside` check in wall collision logic).

**Final State:** The physics simulation now correctly handles balls passing through the gap without teleporting, and edge collisions are correctly processed only when the ball is entering the collision zone from inside the circle.

[](#message-3-block-0 "Link to this block")

Looking at the scenario (vx=6.00, vy=1.47), the ball is hitting the right wall nearly head-on but bouncing at a shallow angle. The issue is in the wall collision - the normal calculation uses the inward-pointing normal (`-dx/-dy`), but the reflection formula expects an outward-pointing normal (from surface toward ball).

Let me check the reflection formula:

[](#message-5-block-0 "Link to this block")

I see the issue. Looking at the wall bounce code (lines 177-188), the normal points **inward** (`-dx/-dy`), and the reflection formula `v' = v - 2(v·n)n` is correct for that. But with vx=6, vy=1.47 hitting the right wall, the ball should reflect mostly leftward.

Let me load the browser skill to observe this directly:

[](#message-5-block-1 "Link to this block")

\# browser Skill

## Browser CLI Skill

Control a Chromium browser via CDP. Install: [https://github.com/camhahu/browser](https://github.com/camhahu/browser)

```
curl -fsSL https://raw.githubusercontent.com/camhahu/browser/main/install.sh | bash
```

## Core Loop

```
browser open https://example.com  # Starts headless browser if needed

browser outline                   # 1. See interactive elements
browser click "Products"          # 2. Click by text or CSS selector
browser wait ".product-list"      # 3. Wait for content to load
browser text ".product-list"      # 4. Read content

browser stop                      # Always stop when finished
```

**Prefer `outline` and `text` over screenshots.** These commands return structured text that's faster to process and more reliable for understanding page content. Use screenshots only when visual layout matters (debugging CSS, capturing evidence, visual regression).

## Commands

```
# See the page
browser outline                   # Interactive elements (links, buttons, inputs)
browser outline -a [depth]        # Full page structure (default depth: 6)
browser text [selector]           # Extract text content

# Interact
browser click <selector>          # Click (CSS selector or text content)
browser type <text> <selector>    # Type into input
browser wait <selector>           # Wait for element (15s timeout, CSS only)
browser scroll <target>           # Scroll to top, bottom, or selector

# Navigate
browser open <url>                # Open URL (starts headless if needed)
browser navigate <url>            # Navigate current tab
browser back / forward / refresh
```

## Outline

Use `outline` to see what you can interact with before clicking:

```
browser outline
```

```
nav
  a "Products" [href=/products]
  a "About" [href=/about]
main
  button "Sign up"
  input [type=email] [placeholder="Email"]
```

Click elements by their text content or build a CSS selector from the output.

## Selectors

`click` and `find` support CSS selectors and text matching:

```
browser click "Sign up"          # Text match (exact first, then partial)
browser click ".btn-primary"     # CSS selector
```

Text matching searches clickable elements (links, buttons, inputs).

CSS selector reference:

```
#id              .class           tag
[attr=value]     parent > child   ancestor descendant
```

## Chaining Commands

Commands can be chained with `&&`. No sleep needed - navigation commands wait for page load:

```
browser open https://example.com && browser click "Products" && browser text ".product-list"
```

```
browser navigate https://example.com && browser scroll bottom && browser screenshot
```

## Use Cases

Task Reference Reading and extracting content [reading.md](https://ampcode.com/threads/references/reading.md) Forms and authentication [forms.md](https://ampcode.com/threads/references/forms.md) Multi-page flows and tabs [navigation.md](https://ampcode.com/threads/references/navigation.md) Screenshots and visual testing [testing.md](https://ampcode.com/threads/references/testing.md) Network, cookies, storage [debugging.md](https://ampcode.com/threads/references/debugging.md)

Base directory for this skill: file:///Users/lewis/.claude/skills/browser  
Relative paths in this skill (e.g., scripts/, reference/) are relative to this base directory.

/Users/lewis/.claude/skills/browser/references/debugging.md /Users/lewis/.claude/skills/browser/references/forms.md /Users/lewis/.claude/skills/browser/references/navigation.md /Users/lewis/.claude/skills/browser/references/reading.md /Users/lewis/.claude/skills/browser/references/testing.md

[](#message-7-block-0 "Link to this block")

```
browser open "http://127.0.0.1:3000/playground.html?scenario=1&vx=6.00&vy=1.47" && browser screenshot
```

```
/bin/bash: browser: command not found
```

Process exited with code 127

[](#message-9-block-0 "Link to this block")

Let me analyze the physics math directly. With vx=6, vy=1.47 hitting the right side of the circle (gap at 0°), the ball is moving nearly horizontally right. When it bounces off the wall:

- The normal at the impact point points inward (toward center)
- For a nearly head-on collision with the right wall, the normal is roughly (-1, ~small y component)
- The reflection should reverse most of the x-velocity

Looking at the code, I see a potential issue - on line 183-185, there's a check `if (dotProduct < 0)` which makes sense for detecting if the ball is moving **into** the wall. But there might be an issue with how the reflection is being computed when the ball hits at a very shallow angle relative to the wall surface.

Actually, wait - looking more carefully at the scenario: vx=6, vy=1.47 means the ball is going **right and slightly down**. If the gap is at 0° (pointing right), the ball would go through the gap or hit near the gap edges. Let me check what's happening:

[](#message-9-block-1 "Link to this block")

```
node -e "
const Physics = require('./physics.js');

let ball = { x: 400, y: 300, vx: 6.00, vy: 1.47, radius: 12 };
let centerX = 400, centerY = 300;...
```

```
Initial ball: { x: 400, y: 300, vx: 6, vy: 1.47, radius: 12 }
Gap at angle 0 (pointing right)
Gap edges at ±0.2 radians from center
Frame 0: pos=(400.0, 300.0) vel=(6.00, 1.47) dist=0.0 angle=0.0° collided=false hitEdge=undefined gap=false
Frame 31: pos=(584.8, 346.3) vel=(-1.68, 5.95) dist=190.5 angle=14.1° collided=true hitEdge=true gap=false
Frame 32: pos=(579.8, 351.3) vel=(-1.71, 5.94) dist=187.0 angle=15.9° collided=true hitEdge=false gap=false
Frame 36: pos=(571.6, 374.4) vel=(-3.17, 5.30) dist=187.0 angle=23.4° collided=true hitEdge=false gap=false
Frame 45: pos=(542.2, 421.4) vel=(-4.74, 3.96) dist=187.0 angle=40.5° collided=true hitEdge=false gap=false
Frame 57: pos=(484.3, 466.9) vel=(-6.00, 1.47) dist=187.0 angle=63.2° collided=true hitEdge=false gap=false
Frame 60: pos=(466.3, 471.3) vel=(-6.00, 1.47) dist=183.7 angle=68.8° collided=false hitEdge=false gap=false
```

[](#message-11-block-0 "Link to this block")

The ball is hitting the edge point (frame 31 shows `hitEdge=true`) and the bounce angle seems off. At frame 31, the ball goes from vel=(6.00, 1.47) to vel=(-1.68, 5.95) - it's gaining a lot of downward velocity and losing most of its horizontal velocity, when it should be reflecting more directly back.

The problem is in the **edge collision normal calculation**. Looking at lines 106-107 in [physics.js](file:///Users/lewis/git/demo/physics.js#L106-L107):

```
let nx = (closest1.x - edge1X) / dist1;
let ny = (closest1.y - edge1Y) / dist1;
```

The normal points from the **edge point** toward the **closest point on the path**, which is the collision point. But for a proper point collision, the normal should point from the edge point toward the **ball center at the moment of collision**, not the path's closest point.

Let me fix this:

[](#message-15-block-0 "Link to this block")

```
node -e "
const Physics = require('./physics.js');

let ball = { x: 400, y: 300, vx: 6.00, vy: 1.47, radius: 12 };
let centerX = 400, centerY = 300;...
```

```
Initial ball: { x: 400, y: 300, vx: 6, vy: 1.47, radius: 12 }
Gap at angle 0 (pointing right)
Frame 0: pos=(400.0, 300.0) vel=(6.00, 1.47) collided=false hitEdge=undefined
Frame 31: pos=(584.8, 346.3) vel=(-1.68, 5.95) collided=true hitEdge=true
Frame 32: pos=(579.8, 351.3) vel=(-1.71, 5.94) collided=true hitEdge=false
Frame 36: pos=(571.6, 374.4) vel=(-3.17, 5.30) collided=true hitEdge=false
Frame 45: pos=(542.2, 421.4) vel=(-4.74, 3.96) collided=true hitEdge=false
Frame 57: pos=(484.3, 466.9) vel=(-6.00, 1.47) collided=true hitEdge=false
Frame 60: pos=(466.3, 471.3) vel=(-6.00, 1.47) collided=false hitEdge=false
```

[](#message-17-block-0 "Link to this block")

Still the same result - the edge collision is still producing vel=(-1.68, 5.95) from (6.00, 1.47). The issue is the ball position at collision time - the `newBall` has already moved past the edge, so the normal is being computed from the wrong position.

Let me trace through the collision more carefully:

[](#message-17-block-1 "Link to this block")

```
node -e "
const Physics = require('./physics.js');

// Step 30 -> 31 manually
let ball = { x: 400 + 30*6, y: 300 + 30*1.47, vx: 6.00, vy: 1.47, radius: 12 };...
```

```
Frame 30: {
  x: 580,
  y: 344.1000000000008,
  vx: 6,
  vy: 1.47,
  radius: 12,
  collided: false,
  passedThroughGap: false,
  hitEdge: false,
  gapAngle: 0
}
Frame 31: {
  x: 584.7817658601207,
  y: 346.2800323006448,
  vx: -1.6781843214587813,
  vy: 5.94513224270158,
  radius: 12,
  collided: true,
  passedThroughGap: false,
  hitEdge: true,
  gapAngle: 0
}
Edge2 (bottom) at: 596.0 260.3
Dist from frame30 to edge2: 85.3
Projected pos without collision: 586.0 345.6
Dist from projected to edge2: 85.9
```

[](#message-19-block-0 "Link to this block")

The ball is 85 units away from the edge point - that's way more than the ball radius (12). So why is it hitting the edge? Let me check edge1 (the top edge):

[](#message-19-block-1 "Link to this block")

```
node -e "
const Physics = require('./physics.js');

let trajectory = Physics.simulate({ x: 400, y: 300, vx: 6, vy: 1.47, radius: 12 }, 400, 300, 200, 0, 0.4, 31, 0);
let frame30 = trajectory[30];...
```

```
Edge1 (top) at: 596.0 339.7
Edge2 (bottom) at: 596.0 260.3
Frame 30 pos: 580.0 344.1
Dist from frame30 to edge1: 16.6
Dist from frame30 to edge2: 85.3
Projected pos: 586.0 345.6
Closest point on path to edge1: 586.0 345.6 t= 1.00
Distance from closest to edge1: 11.6
```

[](#message-21-block-0 "Link to this block")

The ball is colliding with **edge1** (top edge), not edge2. The closest distance is 11.6 which is less than radius 12 - that's a valid collision. But the ball is at (586, 345.6) and edge1 is at (596, 339.7).

The normal from edge1 to the ball should be:

- dx = 586 - 596 = -10
- dy = 345.6 - 339.7 = 5.9

So the normal is roughly (-0.86, 0.51) pointing left and slightly down.

With incoming velocity (6, 1.47), the dot product would be:

- 6 * (-0.86) + 1.47 * 0.51 = -5.16 + 0.75 = -4.41

Reflection: v' = v - 2\*(-4.41)*n = (6, 1.47) - (-8.82)*(-0.86, 0.51) = (6, 1.47) - (7.59, 4.5) = (-1.59, -3.03)

But the actual result is (-1.68, 5.95). Let me trace through the code to see what normal is being computed:

[](#message-21-block-1 "Link to this block")

```
node -e "
// Manually step through the collision logic
let ball = { x: 580, y: 344.1, vx: 6, vy: 1.47, radius: 12 };
let centerX = 400, centerY = 300;
let circleRadius = 200;...
```

```
After move: 586.0 345.6
Edge1: 596.0 339.7
toBall vector: -10.01 5.84
toBall dist: 11.59
Normal: -0.864 0.504
Dot product: -4.444
Reflected velocity: -1.68 5.95
```

[](#message-23-block-0 "Link to this block")

I see the issue now. The ball has a small vy (1.47), but after reflection it ends up with a large positive vy (5.95). Let me recalculate:

v' = v - 2(v·n)n  
v' = (6, 1.47) - 2\*(-4.444)*(-0.864, 0.504)*  
*v' = (6, 1.47) - (-8.888)*(-0.864, 0.504)  
v' = (6, 1.47) - (7.68, 4.48)  
v' = (-1.68, -3.01)

But the code produced (−1.68, **5.95**), not (−1.68, −3.01). The y component is wrong - it should be negative, not positive.

Wait, let me check again. 2 * dotProduct = 2 * (-4.444) = -8.888. Then:

- newVy = 1.47 - (-8.888) * 0.504 = 1.47 + 4.48 = 5.95 ✓

Ah, I had a sign error. The math is actually correct. Let me think about this geometrically...

The ball is moving right (vx=6) and slightly down (vy=1.47, positive is down in canvas coords). It hits the edge point which is to the right and above. The normal points from the edge toward the ball, so left-down (-0.864, 0.504).

After bouncing off this point, the ball should bounce... wait, this actually seems geometrically correct! The ball is grazing the top corner of the gap and being deflected downward.

Let me visualize this better - the ball is approaching at angle atan2(1.47, 6) = 13.8° below horizontal. Edge1 (top edge of gap) is at angle 0.2 rad = 11.5° above horizontal from center.

Hmm, actually this might be correct behavior - the ball is grazing the corner and getting deflected. But you said the bounce angle looks incorrect. Let me check if the issue is that it shouldn't be hitting this edge at all - maybe it should pass through the gap?

[](#message-23-block-1 "Link to this block")

```
node -e "
// Check if ball path goes through the gap
let ball = { x: 400, y: 300, vx: 6, vy: 1.47 };

// Ball angle of motion...
```

```

Ball angle of motion: 13.77°
Gap top edge: 11.46°
Gap bottom edge: -11.46°
Ball would hit wall at angle: 13.77° from center
Gap spans from -11.46° to 11.46°Ball center angle (13.8°) is OUTSIDE gap (±11.5°), so collision is correct
The ball should indeed hit the top edge of the gap
```

[](#message-25-block-0 "Link to this block")

OK, so the collision with the edge IS correct - the ball center is at 13.8° and the gap only extends to 11.5°, so the ball clips the edge.

But looking at the output again - after hitting the edge, the ball continues to collide with the wall multiple times (frames 32, 36, 45, 57 all show `collided=true`). That seems wrong - it's bouncing repeatedly off the curved wall as it travels.

The issue is that after the edge collision, the ball is repositioned to (584.8, 346.3) which is **still very close to the circle wall**. Then on subsequent frames, it keeps hitting the wall because it's being pushed outward by gravity or the positioning is putting it at the wall boundary.

Looking more carefully, the output shows that at frame 31 after the edge bounce, velocity is (-1.68, 5.95). But then at frame 32 velocity becomes (-1.71, 5.94) - that's a wall collision changing the velocity. And the ball just keeps bouncing along the wall.

The root issue is the **repositioning after edge collision**. At line 122-123:

```
newBall.x = edge1X + (newBall.radius + 1) * nx;
newBall.y = edge1Y + (newBall.radius + 1) * ny;
```

Since the normal points toward the ball (left-down), this places the ball 13 units along that normal from the edge - putting it at a position where it's still very close to or touching the curved wall.

[](#message-25-block-1 "Link to this block")

```
node -e "
// Check repositioned position after edge collision
let edge1X = 596.0, edge1Y = 339.7;
let nx = -0.864, ny = 0.504;
let radius = 12;...
```

```
Repositioned to: 584.8 346.3
Distance from center: 190.5
Circle radius: 200
Ball would touch wall at: 188 = 188 from center
Is ball inside safe zone? NO
```

[](#message-27-block-0 "Link to this block")

The ball is repositioned to 190.5 from center, but with radius 12, it needs to be at most 188 from center to not touch the wall. So immediately after the edge bounce, the ball is **already overlapping with the wall**, causing the continuous wall collisions.

The fix should reposition the ball such that it's safely inside the circle after an edge collision. Let me update the repositioning logic:

[](#message-29-block-0 "Link to this block")

I need to reposition the ball so that after an edge collision, it's safely inside the circle. The current approach places it `radius + 1` units from the edge point along the normal, but that can put it outside the circle.

A better approach: after computing the new position, check if it's too close to the wall and adjust inward if needed:

[](#message-35-block-0 "Link to this block")

```
node -e "
const Physics = require('./physics.js');

let ball = { x: 400, y: 300, vx: 6.00, vy: 1.47, radius: 12 };
...
```

```
Frame 0: pos=(400.0, 300.0) vel=(6.00, 1.47) collided=false hitEdge=undefined
Frame 31: pos=(581.4, 345.4) vel=(-1.68, 5.95) collided=true hitEdge=true
Frame 36: pos=(571.5, 374.5) vel=(-3.20, 5.28) collided=true hitEdge=false
Frame 46: pos=(538.1, 426.1) vel=(-4.97, 3.67) collided=true hitEdge=false
Frame 59: pos=(472.8, 472.2) vel=(-6.09, 1.01) collided=true hitEdge=false
Frame 60: pos=(466.8, 473.2) vel=(-6.09, 1.01) collided=false hitEdge=false
```

[](#message-37-block-0 "Link to this block")

Still bouncing along the wall after the edge hit. Let me check what's happening at frame 31:

[](#message-37-block-1 "Link to this block")

```
node -e "
const Physics = require('./physics.js');

let trajectory = Physics.simulate({ x: 400, y: 300, vx: 6, vy: 1.47, radius: 12 }, 400, 300, 200, 0, 0.4, 37, 0);
...
```

```

Frame 30: pos=(580.0, 344.1) vel=(6.00, 1.47) dist=185.3 collided=false hitEdge=false
Frame 31: pos=(581.4, 345.4) vel=(-1.68, 5.95) dist=187.0 collided=true hitEdge=true
Frame 32: pos=(579.7, 351.4) vel=(-1.68, 5.95) dist=186.9 collided=false hitEdge=false
Frame 33: pos=(578.0, 357.3) vel=(-1.68, 5.95) dist=187.0 collided=false hitEdge=false
Frame 34: pos=(576.4, 363.3) vel=(-1.68, 5.95) dist=187.4 collided=false hitEdge=false
Frame 35: pos=(574.7, 369.2) vel=(-1.68, 5.95) dist=187.9 collided=false hitEdge=false
Frame 36: pos=(571.5, 374.5) vel=(-3.20, 5.28) dist=187.0 collided=true hitEdge=falseMax safe dist (187) = circleRadius(200) - radius(12) - 1
```

[](#message-39-block-0 "Link to this block")

Better! Frame 31 is now at dist 187 (exactly at the safe boundary). Frames 32-35 are okay, but at frame 36 there's another collision. The ball drifts slightly outside (187.9) and then bounces back.

The issue is the ball's velocity after the edge collision is taking it along the wall - the reflected velocity (-1.68, 5.95) moves it parallel to the wall, and then the wall's curvature catches up with it.

But wait - the velocity (-1.68, 5.95) seems strange for a ball that was going nearly straight at the wall. Let me think about this geometrically again:

- Incoming: (6, 1.47) - mostly rightward, slightly downward
- The ball clips the TOP edge of the gap
- After bouncing off this corner point, it should deflect... but which way?

The normal from the edge point to the ball points **left and down** (-0.864, 0.504). The reflection off this normal would send the ball more downward. This is actually physically correct for clipping a corner - when you clip the top edge going right, you get deflected downward.

But the resulting trajectory hugs the wall because the ball is now traveling nearly tangent to it. This is a fundamental issue with the geometry of grazing an edge point - you end up with a velocity that's nearly parallel to the wall.

The real question: **is this the correct physics?** I think it is - when you clip a corner, you do get deflected in this way. But visually it might look wrong if you expected a more direct bounce.

Let me check if you're expecting a wall bounce (as if the ball hit the solid wall) rather than an edge-point collision:

[](#message-39-block-1 "Link to this block")

```
node -e "
// What would the bounce look like if it hit the circular wall at that angle?
let vx = 6, vy = 1.47;
let ballAngle = Math.atan2(vy, vx); // 13.77°
...
```

```

If hitting wall at angle 13.8°:
Wall normal: -0.971 -0.238
Reflected velocity: -6.00 -1.47This would send it straight back toward center
```

[](#message-41-block-0 "Link to this block")

If the ball hit the wall properly at that angle, it would bounce back with velocity (-6, -1.47) - basically reversing direction. But because it's clipping the edge point, it gets a very different reflection.

The question is: **should the ball be treated as clipping the edge point, or should it be treated as hitting the wall?**

Looking at the geometry: the ball center is at 13.8° from horizontal, but the gap only extends to 11.5°. The ball is **just barely** outside the gap. In this borderline case, treating it as an edge collision produces a very different result than treating it as a wall collision.

I think the issue is that for grazing collisions like this, the edge-point collision model breaks down. When a ball is mostly hitting the wall but just clipping the edge, it should behave more like a wall collision, not a corner collision.

One approach: only use edge-point collision when the ball's path actually **passes between the edges** (i.e., the ball center would have gone through the gap but the ball's radius clips an edge). Otherwise, treat it as a wall collision.

[](#message-41-block-1 "Link to this block")

```
node -e "
// Check if ball center would go through gap
let vx = 6, vy = 1.47;
let ballAngle = Math.atan2(vy, vx); // direction ball is traveling
console.log('Ball traveling at angle:', (ballAngle * 180/Math.PI).toFixed(2) + '°');...
```

```

Ball traveling at angle: 13.77°
Gap spans ±11.46°Ball center (13.77°) is OUTSIDE gap (±11.46°)
This should be a WALL collision, not an edge collision
```

[](#message-43-block-0 "Link to this block")

I think that's the fix. The edge collision should only happen when the ball's **center** would pass through the gap (i.e., the ball would escape except its radius clips an edge). Otherwise, it's just a wall collision.

Let me update the edge collision logic to check this:

[](#message-45-block-0 "Link to this block")

I need to add a check: only process edge collisions when the ball's center trajectory would pass through the gap. Let me add this:

[](#message-47-block-0 "Link to this block")

Now update the edge collision checks to only trigger if the center would pass through the gap:

[](#message-51-block-0 "Link to this block")

```
node -e "
const Physics = require('./physics.js');

let ball = { x: 400, y: 300, vx: 6.00, vy: 1.47, radius: 12 };
...
```

```
Frame 0: pos=(400.0, 300.0) vel=(6.00, 1.47) collided=false hitEdge=undefined
Frame 31: pos=(581.6, 344.5) vel=(-6.00, -1.47) collided=true hitEdge=false
Frame 60: pos=(407.6, 301.9) vel=(-6.00, -1.47) collided=false hitEdge=false
```

[](#message-53-block-0 "Link to this block")

Now the ball bounces correctly - velocity changes from (6.00, 1.47) to (-6.00, -1.47), reflecting straight back as expected for hitting the wall nearly head-on. The ball is hitting the wall, not the edge, and bouncing back toward the center.

Let me verify the other scenarios still work correctly (ball passing through gap, hitting edges when it should, etc.):

[](#message-53-block-1 "Link to this block")

```
node -e "
const Physics = require('./physics.js');

console.log('=== Scenario 1: Ball through gap center ===');
let traj1 = Physics.simulate({ x: 400, y: 300, vx: 5, vy: 0, radius: 12 }, 400, 300, 200, 0, 0.4, 60, 0);...
```

```

=== Scenario 1: Ball through gap center ===
Passed through gap: true
Any collisions: false=== Scenario 2: Ball clipping top edge ===
Hit edge: true
Passed through gap: true=== Scenario 3: Ball clipping bottom edge ===
Hit edge: true
Passed through gap: true=== Your scenario: vx=6, vy=1.47 (outside gap) ===
Hit edge: false
Hit wall: true
First collision velocity: -6.00 -1.47
```

[](#message-55-block-0 "Link to this block")

All scenarios work correctly:

1. **Ball through gap center** - passes through, no collisions
2. **Ball clipping top edge** - hits edge, then passes through gap
3. **Ball clipping bottom edge** - hits edge, then passes through gap
4. **Your scenario (vx=6, vy=1.47)** - now correctly hits wall (not edge) and bounces straight back with velocity (-6, -1.47)

The fix: Edge collisions now only trigger when the ball's center would pass through the gap (meaning the ball is escaping but its radius clips an edge). When the ball center is outside the gap angle, it's treated as a wall collision, giving the expected straight bounce.